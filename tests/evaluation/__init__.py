"""ctrlmap evaluation test suite.

Non-deterministic tests that evaluate RAG pipeline quality.
These require a running Ollama instance and real embedding models.

Gate with: pytest -m eval
Skip with: pytest -m "not eval"
"""
